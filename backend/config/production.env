# Server Configuration
NODE_ENV=production
PORT=10000

# MongoDB Connection - Set in your deployment environment
MONGODB_URI=${MONGODB_URI}

# JWT - Set in your deployment environment
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRE=30d
JWT_COOKIE_EXPIRE=30

# File Upload
MAX_FILE_UPLOAD=5242880 # 5MB in bytes

# Frontend URL (for CORS and email links)
FRONTEND_URL=https://gideons-tech-suite.onrender.com

# CORS Whitelist
CORS_WHITELIST=https://frontend-t73t.onrender.com,https://gideons-tech-suite.onrender.com,http://localhost:3000
